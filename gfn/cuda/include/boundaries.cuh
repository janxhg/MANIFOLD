#ifndef BOUNDARIES_CUH
#define BOUNDARIES_CUH

// SHIM: Redirect to new Modular Architecture
#include "manifold_metrics.cuh"

// Macros for compatibility if needed (TOPOLOGY definitions match enum values)
#define TOPOLOGY_EUCLIDEAN 0
#define TOPOLOGY_TORUS 1
#define TWO_PI (2.0f * PI)

#endif // BOUNDARIES_CUH
