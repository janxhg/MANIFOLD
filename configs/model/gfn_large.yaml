# GFN Large Model Configuration (50M+ Parameters)
# For benchmarking and scaling experiments

model:
  vocab_size: 16
  dim: 1024
  depth: 24
  rank: 256
  heads: 16   # Multi-Head Manifold
  integrator: "leapfrog"
  use_scan: true
  use_adjoint: false  # Disabled for Speed

# Computed: ~56M parameters
